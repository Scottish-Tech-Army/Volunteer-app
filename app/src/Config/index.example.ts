/**
 * @file Example config settings -- duplicate this file and create an index.ts file in the same directory
 */

export const Config = {
  // Set this to the tunneled URL of your local API
  STA_BASE_URL: '',

  // Prefix that specifies current API version.
  STA_API_VERSION: '/v1',
}
